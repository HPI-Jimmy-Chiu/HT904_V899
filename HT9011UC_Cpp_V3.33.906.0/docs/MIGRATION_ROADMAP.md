# HT9045 V906 BCB6 → C++ 全面遷移順序（ROADMAP）

> 目標：把 `HT9011UC_Code_V3.33.906.0_20260618`（BCB6/VCL C++，唯讀黃金參考）**全部**翻譯成標準/Visual C++。本檔是總路線；逐步紀錄見 `DEVLOG.md`，知識/gotcha 見 `KNOWLEDGE.md`。

## 關鍵策略（keystone decisions）
1. **VCL-compat 層（最關鍵）**：在 `vclcompat/`（附加目錄）自製「只實作專案實際用到的 API 子集」的相容層——`AnsiString`（**1-based** `Pos`/`SubString`/`Length`/`UpperCase`/`Trim`/`sprintf`/operator+ 等，語意完全照 BCB6）、`TStringList`、`TDateTime`、SysUtils 風格自由函式（`IntToStr`/`StrToInt`/`Format`/`FileExists`/`Now`/`FormatDateTime`…），以及巨集中和（`__fastcall`/`__closure`→空、`__property`→getter/setter 指引、`PACKAGE`/`DELPHICLASS`→空）。**好處**：大量檔案可忠實、保名、低風險地機械翻譯（避開 ~1,910 個 1-based off-by-one 改寫），日後可選擇性重構成 idiomatic std。翻譯檔以 `#include "vclcompat/vcl_compat.h"` 取代 `<vcl.h>`。
2. **檔案鏡射**：翻譯出的 `.cpp/.h` 鏡射 BCB6 相對路徑+檔名（見 KNOWLEDGE）；附加檔（`vclcompat/`、`CMakeLists.txt`、`tests/`、`docs/`）才用新結構；`.bpr`→CMake。
3. **建置/驗證**：CMake 鏡射樹 + MinGW g++（本機無 MSVC；最終 MSVC build 在開發機）。每模組有 golden oracle 就對拍（MD5 向量、真實 config/log 檔、手算公式值）；無 Borland 故不對原 binary live diff（已知限制）。
4. **硬體/UI 接縫**：即時硬體核心保留 native、同程序直呼；硬體與 UI 走抽象基底 + Sim/Real、Stub/真UI（§9 HAL）。**UI 框架（MFC/Qt/Win32）延到 W7 再定**（不擋 W0–W6）。
5. **節奏**：一波一波、一個模組一個 workflow；每批完成自動 commit + 更新 DEVLOG/KNOWLEDGE/RESUME。這是長期多輪工程。

## 翻譯順序（低風險/底層 → 高耦合/UI）
- **W0 — 基礎**：`vclcompat/` 相容層 + CMake 鏡射骨架 + 翻譯全域定義/型別標頭（`MachineType.h`、`cmydef.h`、`cprod.h`、`cpublic.h`、`MachineDefine.h`…）去 VCL 化。**出場**：基礎可編、compat 層單元測試過。
- **W1 — Public/ 葉節點工具**：`cJSON`(純C)、`HTMD5`(MD5 標準向量對拍)、`MyStringList`、`HonNewDel`、`MemoryAlarm`、`WinSocketErrorCode`、`HTMD5`、`systools` 等。**出場**：各自可編 + golden 測試。
- **W2 — 純邏輯島**：`Common/PickPlanner`(cArmPickPlan/cInArmLoaderPickPlanner/cOutArmPlacePlanner)、`SortingBinTray`、`cUnitConvert`、`ContactForce`(✅已翻計算核心) + `cContact` 計算消費端、`ContactForce::ReadFile`。
- **W3 — 設定/DB/基礎服務**：`cinitial`(ini/csv/dat 讀取，名稱式解析)、`cMyDB`+sqlite3(可攜C)、`database.cpp`、`common.cpp`(SGDToCSV 等)、`ExternFunction`、`handlerlog`、`language`。
- **W4 — 硬體抽象層(HAL)**：`HTMotor` 基底 + `mymotor` 派發 + 各廠牌馬達(CSMC/Galil/MN200/Syntek/EtherCAT) 後接抽象基底 + Sim；`myio`/`MyLaneIo`/`myswitch`/`mysensor`/`mycylin`/`mykitsuck`(IO)；KeyPro shim；tester(GPIB/TCP/Sim)；`TempCtrl` 驅動。廠商標頭唯讀；real 接 native、Sim 供離線。
- **W5 — 通訊/協定**：`SECSGEM`(uHGemHT9045…)、`Interface/TesterTCP`/`InterfaceSYS`、`KYECFTP`、`Automation` 網路、`CanBus`/`CCLink`/`MyPLC`/`EtherCAT` 通訊、~~`MR/RFID`~~(**2026-07-11 recon 確認：MR 目錄不在 HT9045.bpr FILELIST，全目錄死碼，移出範圍**)、`BarCode`(原不在計畫內，recon 順便評估後併入)。**◐ Wave 1 完成(2026-07-11)**：SECSGEM 基底層+SECS_EVENT、Interface/InterfaceSYS、CCLink 驅動對、CanBus、MyPLC(+新 vclcompat TCP client shim)、EtherCAT 通訊層、Automation 的 AMR/AGV predicates/HANA_ART/uRENESAS_Other/SCK_ART 部分——見下方進度與 DEVLOG 2026-07-11。**◐ BarCode Wave 1 完成(2026-07-11)**：`TfBarCode_Shim`(實際 20 方法，先前估 17)18/20 接上真本體，翻轉 4 個既有反向依賴引擎(acarry/ainarm9045/aoutarm9045/csystem)行為，見 DEVLOG 2026-07-11 BarCode 區段。**✅ W5-Final 完成(2026-07-11 執行/2026-07-13 復原+commit)**：KYECFTP(`ht9045_kyecftp` 新 library)、Automation 剩餘(`auto9045.cpp` 148函式/`AGV_E84.cpp` E84握手核心/`SCK_ART_Remainder.cpp`)、`Interface/TesterTCP_Socket.cpp`(有界 socket 管理子集)、BarCode 收尾(`TfBarCode_Shim` **20/20 真本體達成**)、SECSGEM `THGem` 前置切片(`SecsWireCodec`+`SecsSvEcRegistration`)——見 DEVLOG 2026-07-11/2026-07-13 W5-Final 區段。**✅ SecsWireCodec Wave 2 完成(2026-07-13)**：延伸 `SecsWireCodec` 加 15 個訊框 header 組/解+SML byte-stream 解碼器函式+2 前置依賴，見 DEVLOG 2026-07-13 區段。**✅ 三前線同波完成(2026-07-13，「workflow 火力全開」)**：`uHGemClass.cpp` 10/57 gated method 解鎖(WireCodec+SvEcReg 埋為 by-value 成員)、`AGV.cpp` 剩餘 13 函式全清(全檔狀態機邏輯已無剩餘可翻)、新 `vclcompat/ServerSocket.h`(TServerSocket 多連線 shim，2 個具體 golden 呼叫點為據，尚無真消費端)——見 DEVLOG 2026-07-13 三前線波區段。**✅ 2026-07-16/17 三連波再推進**：`uHGemEquipment.cpp` StringGrid CEID/Report/Alarm 資料庫(26 method，`THGem` 首建)+連線生命週期 A/B 兩桶(clientGem/srvGem 開通+ini 持久化+狀態面板)皆完成；`automation.cpp` 網路層 41 method 完成；`Interface/TesterTCP.cpp` 第二個有界子集(OS-recipe zip 對)完成；`uHGemClass.cpp` 解鎖至 13/57。**剩**：`uHGemEquipment.cpp` C 桶(`Timer1Timer`本體+`clientGemRead`/`ProcessSocketReceiveData` 協定解碼核心，需新 `TCriticalSection`/`TMemoryStream` shim)+KYEC 真計時 `HTimer`、`uHGemClass.cpp` 剩 44 個 gated method(多卡 VCL widget/SV-EC 註冊家族)、站點覆寫層(`uHGemHT9045.*`)、`Interface/TesterTCP.cpp` 的 `TimerProcessTCPDataTimer` 核心、`automation.cpp` 的 `ProcessBuffer`/`ProcessBuffer1`/`SendReportRequest` 派工核心(~47% golden 行數)。
- **W6 — 邏輯狀態機（root，最大宗）**：`ainarm9045*`/`aoutarm9045*`/`atester*`/`asendic*`/`acatchtray`/`ShuttleMove`/`auto9045`/`automation`、`csystem`/`cprod`/`cpublic` 邏輯、`ckernel`；切 `Prod`/`iXXXTask` 全域狀態邊界。
- **W7 — UI（133 .dfm 表單）**：先定 UI 框架（MFC 最近 VCL / Qt 現代 / Win32）＝**獨立大決策**；逐表單重建於 view 介面後，接已翻邏輯。最大、最後。

## 進度
- ✅ C++ pivot、檔案鏡射慣例、ContactForce 計算核心（W2 先行示範）。
- ✅ **W0 基礎完成**：vclcompat 層（AnsiString 1-based 等）+ CMake 鏡射骨架 + Public/cJSON + Public/HTMD5；ctest 4/4、MD5 命中 RFC1321。
- ✅ **W1 第一批 Public 葉工具**：`ExternFunction`(純函式部分)、`WinSocketErrorCode`(GetErrorMsg)、`cBootLog`；ctest 7/7。
- ✅ **W2 邏輯島（部分）**：`Common/PickPlanner` 通用引擎 cArmPickPlan（與 golden 幾乎逐字同，僅換 include）+ 兩個 scaffold adapter（gated off）；`cUnitConvert` 純轉換部分。ctest 9/9。
- ✅ **W0 尾段＝全域標頭完成**：`MachineType.h`/`Config.h`/`CosFunction.h`/`myTimer.h`/`cpublic.h`/`cprod.h`/`cmydef.h`/`MachineDefine.h` 全部去 VCL 化、**單一 TU 一起編可過**（ctest 11/11，含 globals：PROD_INFO_ST 的 static_assert/offsetof 錨點 + enum/#define 值對拍）。**瓶頸已解除**。form 指標前置宣告→W7；.cpp 觸及狀態機的 body `#if 0`→W3/W6。
- ◐ **W3 config/DB — 前置已備，readers 待續**：已加 vclcompat 檔案系統 API（FindFirst/FindNext/FindClose/TSearchRec/fa*/RemoveDir/FileSetAttr/FileGetAttr/HexStrToInt）、`third_party/sqlite3/` 外部相依 scaffold（現 header-only stub；待 vendor amalgamation）、`tests/test_config_db.cpp` 真檔 oracle（鎖定 45 SMC/MOTION=1/IO=2/IO_Table 644:24）、修 vclcompat TStringList SetCommaText/SetDelimitedText 尾分隔重複計數 bug。ctest 12/12。
- ✅ **W3-cont 閘門完成**：vclcompat `TIniFile`/`TMemIniFile`（IniFiles.h/.cpp，write-through/eager-load、Big5-safe、case-insensitive、default-fallback；43/43 shim 測試）+ `common.cpp` ini-helper 家族 38 函式（OpenGeneralIniFile/CheckAndReadIniDataGeneral 多載/WriteIniDataGeneral）。**真檔 oracle 過**：經譯後 helper 鏈讀真 Gerneral.ini 得 MOTION=1/IO=2/TTL=2/INDEX=0/HEATER=4、missing→default。ctest 14/14。
- ✅ **W3-cont2 config-table loaders 完成**：database.cpp 的 LoadMotData/LoadIoData/SetMOTTableNo/SetIOTableNo/TIODATA/TMOTDATA（8 函式）翻譯，**真檔 oracle 過**（45 列全 SMC、名稱式 col 解析證實、IO 668 列 ISABase 644:24、M00→MInArmX）。Win32 巨集 guard 進 vcl_compat.h。修 AnsiString 缺 operator=(int) bug。ctest 15/15。
- ✅ **W4 HAL 馬達層完成（interface-cut 離線證明）**：HTMotor(虛擬基底)+TMyMotor+新 `TMySimMotor` 翻譯，**離線零 vendor SDK 可編可跑**（test_sim_motor 33/33：home→0、MoveToPos(N)→N、no alarm、MotorHome 狀態機收斂；nm/strings 稽核零 vendor 符號）。**6 個品牌驅動 → W4-part2**（每個 .cpp 都拉 ~30 模組 god-stack [main/csystem/sensors/cylinders]，故卡在 W6，與 vendor SDK 無關；Sim 先頂著）。mymotor 大量 motion/Galil/sensor 方法 gated TODO(W6)、pHTray→W7。ctest 17/17。
- ✅ **W4-IO HAL 完成（IO interface-cut 離線證明）**：`IOBackend.h/.cpp`（TIOBackend 虛基底 + TSimIOBackend 離線；real backends mn_*/Acm_*/_mnet_*/raw `#if HAVE_xxx` 預設 OFF）+ `MyLaneIo`(facade，6 method 經 pIO 派發、保留 range/OutPortData 記帳) + `myswitch`(TMySwitch)/`mysensor`(TMySensor) 物件層路由到 facade。test_sim_io 等通過、ctest 19/19。
- ✅ **HAL enablers：KeyPro shim + TComm serial shim**：`Public/HTKeyProShim`(KeyPro_GetLevel→LoadLibrary/GetProcAddress，離線回 1，非靜態連結；decoration 為 undecorated 已 objdump 確認) + `vclcompat/Comm`(TComm 14-method，Win32 \\.\COMx + SIM 模式)。ctest 20/20。HAL Sim 層：馬達✓/IO✓/license✓/serial✓。
- ▶ **下一步：ungate cpublic.cpp 基礎**（queue/VerInfo/DTK4848 溫控協定/簡單 util——只依賴 globals+vclcompat+TComm 的部分；god-stack 部分續 gate）。解鎖：移除 queue-ctor stub workaround + 提供溫控協定。之後 W5 comms、W6 root 狀態機（解鎖最多 deferred）、W7 UI。
- ⏳ 待：W4-part2 品牌馬達驅動(需 W6)、cylinder/sucker 狀態機+TMyKitSuck(W6)、database.cpp ReadGeneralIni/SECS、cMyDB(待 vendor sqlite)、MyStringList、common.cpp 其餘、W5 comms、**W6 root(見下方計畫)**、W7 UI。

## W6 計畫（root 狀態機，recon 產出）
**拓樸**：單根 fan-out（非 peer mesh）。`csystem::MainProc()→DoAllProcess()` 每 tick 固定序 pump：DoLoad→DoInArm→Do_Auto_SHT1/2/3→DoTestHeadMotor→DoCatchTray→DoOutArm/DoSortArm。`csystem.cpp` 25,483 行 god-file（~257 Prod、**301 fMain**）。`iXXXTask` 非中央表，各 arm 自有 cursor（iArmTask@ainarm2、OutArmTask@aoutarm、iTestHeadMotorTask@atester、AutoSHTnTask@acarry、CatchTrayTask@acatchtray、**iIndexTask@cContact**）。SM 間**不互相 Do-call**，只透過 `csystem.h` predicate API(IndexHasIC/ShuttleHasIC/TestSocketHasIC…)協調。真正阻礙＝`main.h/TfMain`(每個 .cpp include、heavy 模組 deref fMain->)。
**解耦三縫**：(1) `csystem.h` 凍結為介面 + 薄 `csystem_predicates.cpp`(predicate 實作於 Sim HAL+Prod)；(2) iXXXTask 各 SM 自帶 cursor；(3) `main.h/TfMain`→非 VCL **FormsFacade**(只暴露 SM 用到的 run-control 方法 Pause/Home/Start/DoStateRecord/SendMSG_CMD + cInplace=cInArmPlacement 邏輯)+ satellite 表單 stub。全部靠既有 Sim HAL 讓 SM 無 W7 也可編可跑。
**子波序**：
- ✅ **W6.0 SCAFFOLD 完成**：csystem.h 介面 + csystem_predicates.cpp(15 HasIC over Sim HAL) + aArmHeader.h shim + 非 VCL FormsFacade(TfAGV) + 新 mycylin(TMyCylinder over Sim IO) + asendic/canary_support helpers。
- ✅ **W6.1 CANARY 完成（策略證明）**：`asendic_Empty` 翻譯+Sim HAL 收斂(ctest 23/23、mojibake 0)。三縫(predicate 介面/FormsFacade/自有 cursor)端到端有效。剩餘 canary 葉(asendic_Auto_RT/Auto2/Loader_RT)可隨後批。
- ✅ **W6.2 IN-ARM 基礎完成**：`ainarm_SearchPickPlate`+`ainarm_SearchPlacePlate` 翻譯；新 `aHotPlateSubstrate`(InArmSuck=TMyKitSuck/PickFromHPList/ainarm2 cursors+HP arrays，最小 scope 部分材料化原 W6 deferred KitSuck grid)；FormsFacade 擴 5 form；DoInArmPickFromHotPlate_9045 Sim HAL pump 過(cursor 守 documented set、fall-through 保留)；DoPlaceToHotPlate_9045 dispatcher gated#if0→W7 stub。ctest 25/25、mojibake 0。
- ✅ **W6.2-CORE IN-ARM 引擎完成**：`ainarm9045.cpp`(9222 行,302 iInArmType 分派)+`.h` 忠實翻譯；3 核心 SM(AdditionalFunction/IonFanGiveWay/SCKARTLoadingCount) Sim HAL pump 過；DoInArm_9045/_SuckerMap 分派 ladder ACTIVE；21 `#if 0` gate 全平衡(25 variant arm→W6.2b、GetShuttleCol 表→W7、4 整體 SM→W7)；8 oracle 過(含 float→int 截斷)。ctest 26/26、mojibake 0。
- ✅ **W6.2-OUT 出料臂引擎完成**：`aoutarm9045.cpp`(golden 4753 行)+`.h`(in-arm 鏡像)；4 核心 SM(PlaceToAuto/AfterPlaceToAuto/AdditionalFunction/IonFanGiveWay) Sim HAL pump 過；DoOutArm_9045/DoPickFromShuttle_9045 分派 ladder ACTIVE；substrate 擴 OutArmSuck/OutArm2Suck/FRCarryKit/BRCarryKit；22 #if gate 全平衡；oracle 過(truncation/零除)。ctest 27/27、mojibake 0。
- ✅ **W6.3 CATCHTRAY (TrayArm) 引擎完成**：`acatchtray.cpp`(golden 9142 行)+`.h` 忠實翻譯；6 核心 SM 全 ACTIVE(中央 DoCatchTray 分派 5 子 SM DoCatchFromLoader/CatchNewTrayFromBuffer/DoPlaceTrayToAuto/DoPlaceToBuffer/DoSlapTray)；**config 分派**(TRAY_ARM_MODE/TrayForm/IniConfig)非變體分派；DoSlapTray/DoCatchTray Sim HAL pump 過；5 oracle 過(+100/-100 整數視窗等)；只有 2 真 #if0(BinDisCtrl→W7-UI)、32 SOFT_SIMULTE verbatim、36/36 平衡；新 `acatchtray_shims.{h,cpp}`(fTrayMapping/Magazine/OCR/AMR/cassette gated)；WhichAutoNeedTray ODR 解決(真本體移入 acatchtray.cpp、canary stub 刪)。ctest 29/29、mojibake 0。
- ✅ **W6.4 TESTER/TEST-HEAD (Index) 引擎完成**：`atester.cpp`(golden 11819 行，最大 TU)+`.h` 忠實翻譯。核心 test-head SM ACTIVE 並 Sim HAL pump 過：`DoTestY`(iTestYTask 1→50→100 FALL-THROUGH 110)、`DoTestHeadMotor`(iTestHeadMotorTask **4→9→10→15** over Sim HAL，Motor==NULL 快路徑)。`Check_TTL_Status`/`CheckTestSocketIsError`/`IndexEveryTimeCheckEP` decode 消費者 ACTIVE。5 oracle 過(ArrayConvertSite/GetRowCol 幾何 + GetIndexZSpeed 整數除法 50000*30/100==15000、scale=0 clamp→500)。49/49 directive 平衡；GetTesterResult(:849)/ProcessTestResult/DoCheckSocketHasIC/torque/EP/load-cell + DoTestHeadMotor 下壓 case 樹(6232-8655,含 cContact iIndexTask) 全 `#if 0`→W5/W7；32Site/Front/Rear/ProcessCount/fContact/ADAM_* 走 `atester_shims`。Motor==NULL Galil-Z 快路徑(無回歸)。新 test `test_w6_4_tester`(W6_4_Tester)。ctest **30/30**(clean from-scratch)、mojibake 0。
- ✅ **W6.5 SHUTTLE/CARRY 引擎完成**：`acarry.cpp`(golden 8521 行)+`.h` 忠實翻譯。**case 覆蓋率 136/136 ACTIVE(100%)、acarry.cpp 內 0 個 `#if 0`**(本波最高覆蓋——低耦合:0 tester-comm/0 cContact/fMain 走 FormsFacade，故 shuttle 移動 case 全 ACTIVE)。核心 SM `Do_Auto_SHT1`(AutoSHT1Task)entry 1→10(golden:3342)、64-tick pump 留在記錄 switch 集無逃逸；`Do_Auto_SHT2`/9046AU `Do_Auto_SHT3` ACTIVE 連結；case 200→201 fall-through verbatim。5 oracle 過(12 斷言)：`GetShuttle1Pitch_9045` double→int **截斷 11001 不進位 11002**、`CheckShuttleOutputHasICError` 零偵測位防護回 false、`CheckShuttleSensorBroken_1` 左向 pass 回 false。uShuttleThread SM 邏輯 verbatim、Win32 wrapper 薄離線 driver。新 `acarry_shims.{h,cpp}`(Tech/fLtcSensor/OutSht3Kit/ATC/floating/sort-arm 全 ACTIVE 離線本體)；substrate 擴 TMyKitSuck(SetAllRealIC2InterfaceBin/CountRealIC/HasDefineIC 忠實本體 + grid + TSoakTime)/TfBarCode_Shim(17 shuttle method)/LastSet.iShuttlePurgeCount；ODR：IsFLCarrKitAllHasIC/IsBLCarrKitAllHasIC 真本體歸 acarry.cpp、substrate placeholder `#if 0` 移除。**SOFT_SIMULTE 11/11 與 golden verbatim 全等**(本波無 gate 大 SM)。新 test `test_w6_5_shuttle`(W6_5_Shuttle)。ctest **32/32**(clean from-scratch)、mojibake 0。
- ✅ **W6.6 csystem ORCHESTRATION HUB spine 完成（🏁 W6 CORE LAYER 完成）**：`csystem.cpp`(golden 25,483 行 god-file)整合 SPINE 翻譯——`DoAllProcess()`(golden:9115) 主迴圈把 W6.0→W6.5 五引擎(in/out-arm/tray-arm/tester/shuttle)以記錄 A/B tick 序**串起來 pump over Sim HAL**：TICK A(bDoProcess==true)=SensorScan→DoLoad→DoInArm→Do_Auto_SHT1→DoTestHeadMotor→DoCatchTray；TICK B(false)=SensorScan→DoLoad→Do_Auto_SHT2→DoTestHeadMotor→DoCatchTray→DoOutArm；function-static bDoProcess 每 tick toggle(golden:10254)。`MainProc()`(golden:16711) 薄外層→DoAllProcess+DoOneCycleFinishCheck+DoCleanOutFinishCheck(golden:18728-18740)。ACTIVE spine 翻 **672 行 / ~16 方法**(DoAllProcess skeleton verbatim+8 master guard+5 引擎、MainProc 薄 head、alive-instrumentation、InitAllProcessTask/CheckMotorHome/SetInitialICCheck/InitOneCycle/InitCleanOutFunction/InitTrayEndFunction) vs golden **~243 函式**(≈6% 方法 / ~2.6% 行)——**spine 波預期的少數覆蓋**。新 `csystem_shims.{h,cpp}`(DoInArm 真→DoInArm_9045、DoOutArm 真→DoOutArm_9045、DoLoad/DoSortArm/Init* no-op、finish-check no-op，全 ACTIVE 無 `#if 0`)。**ODR：csystem.cpp ∩ csystem_predicates.cpp = 0**(HasIC 家族留 predicates、5 引擎 Do* 永不重定義)。**SOFT_SIMULTE：golden 50 處全在 gated 大宗 ladder 內、隨本體一起 gate、無一改寫；active spine 本就 0 處→譯檔 0 忠實**。真 line-leading #if/#endif **7/7 平衡**。新 verify TU `tests/test_w6_6_csystem_cycle.cpp`(W6_6_CSystemCycle)：4 oracle(O1 tick-序逐字相等/O2 instrumentation CallCount 0→5+alive/O3 master-guard 早返只 [SensorScan]/O4 64-cycle pump=32A+32B 嚴格交錯無 crash) **20 PASS/0 FAIL**；既有 W6_6_Hub 續綠。ctest **34/34**(fresh from-scratch `build_w6_6_verify`，最大 build、無回歸)、mojibake 0(全 6 檔)。
- ✅ **W7 T1 完成**：`atester.cpp` `DoTestHeadMotor()` 下壓/test/torque/EP/load-cell case 樹(golden:6229-8650,107 case)un-gate+忠實翻譯，取代 default-park stub；修正 W6.4 遺留 `iCASE_REAL_CCD2..6=9` ODR collision landmine(改回 golden file-scope 40200..40510)+ `IndexEveryTimeCheckEP` bool 簽名；全套 ctest 46/46、mojibake 0、獨立 fidelity-review agent 逐行對 golden zero discrepancies。詳見 DEVLOG 2026-07-06。
- ✅ **W6.4b recon + Wave 1 完成**：5 檔(atester_32Site/atester_ProcessCount/aTester_Front/aTester_Rear/cContact)recon 完成，**修正本檔先前錯誤標籤**——`iIndexTask@cContact` 不存在，真正下壓 cursor 是 `TfContact::DoTestContactFunction` 的 `CarlibrationTask`(cContact.cpp:11755-13963,2209行,~78 case)+6 個私有 sub-SM(`DoZ1/Z2PickFromShuttle`/`Do_Z1/Z2_AutoGetHeight`/`Do_ContactTest_32Site`/`DoZPlaceToShuttle`)，共~11347行≈cContact 22761行的 50%，VCL widget 存取穿插 switch-case 內部(比 T1 更破碎)，需獨立 W7 波。**Wave 1**：`aTester_Front.cpp/.h`+`aTester_Rear.cpp/.h` 新增，`DoFrontTestDestroyIC`/`DoRearTestDestroyIC`(golden :309-865/:309-886)+6 葉依賴忠實譯入，取代 atester_shims.cpp 的 100% offline stub；integrate 順帶修 2 個真 undefined-reference cross-module 缺口(`OutSHT1/2InLF`+`CheckIndexAllSuckICFallDown`補進 csystem_predicates.cpp)；全套 ctest 47/47、mojibake 0、獨立 fidelity-review agent zero discrepancies。詳見 DEVLOG 2026-07-06。
- ✅ **W6.4b cContact 純算對完成(2026-07-10)**：`GetMinForce`(cContact.cpp:18957-19022)+`GetMaxIndexForceLimit`(18893-18955) 抽成自由函式 `ComputeMinForce`/`ComputeMaxIndexForceLimit`(新 cContact.h/.cpp，鏡射 root，extract-calc-core 比照 ContactForce 前例，零 ht9045_globals link 依賴)。忠實保留 2 個 golden 缺口(ItemIndex 無 trailing else 停 0.0；dMinForce member→純區域變數，對唯一呼叫點行為等價，member 跨呼叫點持久語意記 DEFERRED)。主迴圈獨立複驗(兩輪 fresh from-scratch，含 stash 前後對照)：ctest 45/48 PASS(新增 cContact 套件 100%、既有 3 個 config_db/IniFiles/ini_helpers 失敗經證實為現場 `system/Gerneral.ini` 已改值造成的既有環境漂移、與本波無關非回歸)；獨立 fidelity-review agent 8 項檢核零差異。詳見 DEVLOG 2026-07-10。
- ✅ **W6.4b 收尾完成（2026-07-11，隨 W5 Wave 1 一起落地）**：`atester_ProcessCount.cpp` 部分翻完(14 函式，golden 2161 行取 ~1234 行乾淨子集；翻譯 agent 回報階段 API 連線中斷，但檔案本身完整可用，integrate agent 主動核實磁碟內容後正常併入)；Front/Rear Suck 對(`DoFrontTestSuckIC`/`DoRearTestSuckIC`)完成，擴充 W6.4b Wave 1 既有的 `aTester_Front.cpp`/`aTester_Rear.cpp`。**W6.4b 至此僅剩 `atester_32Site.cpp` 深層 COM2/RTC-vision 孤島(已隨同波翻完整檔、僅 2 個明確 gate 島延後) + cContact 下壓叢集(`CarlibrationTask`+6 sub-SM，仍是獨立 W7 大波，不受影響)**。詳見 DEVLOG 2026-07-11。
- ✅ **W5 Wave 1 完成（2026-07-11）**：9 路 recon（含主迴圈核實 SECSGEM.cpp/MR 全目錄為死碼，不排入任何波次）→ 2-batch 平行翻譯戰役（comms 叢集 6 unit、automation+atester 家族 8 unit，14/15 完成、1 個 API 崩潰但 integrate 從磁碟救回）→ 2 個序列 integrate → 主迴圈兩輪獨立複驗。新增：`SECSGEM/uHGemClass`(`HTGem` 基底,57/90 方法 gated 待未來 THGem 波)+`SecsEventType`(`SECS_EVENT`)+`SecsEventReport`(極簡 Sim EventReport，接上 csystem.cpp 既有 W7C1/W7C2 EventReport 占位)；`Interface/InterfaceSYS`(併入 ht9045_sm)；`CCLink/{MyCCLink,MyCCLinkBoard,MyCCLinkSensor_predicates}`；`CanBus/cMyNUDN1`；`MyPLC/{MyPLC_IO_Modbus,ModbusTCPClient}`+新 `vclcompat/ClientSocket`(本波唯一新 vclcompat 元件)；`EtherCAT/MyNUEC1`；`Automation/{AMR,AGV_predicates,HANA_ART,uRENESAS_Other,SCK_ART(部分8函式)}`；`atester_32Site.cpp`(整檔，僅 2 個 vision 孤島延後)。**最高槓桿發現**：`AGV_predicates`(`IsSPIL_AMR`/`IsATK_AMR`/`Use_AMR`)接上 FormsFacade 後，`acatchtray.cpp` 現有 ~18 個呼叫點從恆假 stub 首次評估真條件——主迴圈已逐字元核對 golden 相符。ctest 58/61(3 個既有無關失敗、無回歸)、mojibake 0(68 檔)、真新增警告僅 ~29 筆全數核實非缺陷(720 表面數字中 668 筆是既有 baseline 噪音，非本波引入)。詳見 DEVLOG 2026-07-11。
- ✅ **Automation 網路層+TesterTCP OS-recipe 對+uHGemEquipment 首建+uHGemClass 再解鎖 3 個完成（2026-07-16 執行/2026-07-17 補記+複驗+commit，第三次 workflow crash 復原案例）**：新增 `Automation/automation.cpp/.h`(`TfAutomation`，44 method+6 free func，41 真譯/3 gated stub：`ProcessBuffer`/`ProcessBuffer1`/`SendReportRequest` 仍留白，約佔 golden 47%)、`Interface/TesterTCP.cpp/.h`(3 函式：OS recipe zip 收發對+`btnSaveClick`，與既有 `TesterTCP_Socket.cpp` 不重疊)、新 `vclcompat/StringGrid.h/.cpp`(headless TStringGrid shim)+`SECSGEM/uHGemEquipment.cpp/.h`(`THGem` 類別首建，CEID/Report/Alarm StringGrid 資料庫家族 26 method，忠實保留 2 個 golden bug：`GetCEIDContent`/`GetReportIDContent` 誤用 RowCount、`ReadAlamData` 例外路徑 use-after-free)；`uHGemClass.cpp` 再解鎖 3 個(10/57→**13/57**，`GetDataItemLenAndTypeAndDelete` cluster 3/17 已解)、`SecsWireCodec` +3 method。4 路平行稽核+主迴圈親自複驗：ctest 83/87(4 個既有無關環境漂移失敗)、mojibake 0(18 檔)。詳見 DEVLOG 2026-07-16/2026-07-17 區段。
- ✅ **uHGemEquipment 連線生命週期 Bucket A+B 皆完成（2026-07-17，同日連續兩波）**：recon 修正 ROADMAP 原估「~50 method TServerSocket+Timer1Timer」實跨 A/B/C 三桶——`Timer1Timer` 本體、`DoConnect`/`DoSelect`/`DoSeparate`、`DoProcessSFNoResponse`、`clientGemRead`/`ProcessSocketReceiveData`(真正協定解碼核心)全落 C 桶(需未設計的 `TCriticalSection`/`TMemoryStream` shim + `SecsWireCodec` 成員化)，留給未來波。**A 桶**：新增 `clientGem`/`srvGem` 成員+連線狀態旗標，翻譯 28 個函式(`clientGem*`/`srvGem*` event handler、`DoOpenCommuncation`、`DoOnLine`、`Connect`/`DisConnect`/`OnLine`/`OffLine` 家族、`ClearDefaultEvenReport`、`GetTimeInfo`半段、`SaveSECSGEMErrToLog`)——`vclcompat/ServerSocket.h`(2026-07-13 就緒)首個真消費端；翻譯期間修正 1 個真 use-after-free(解構子順序)。**B 桶**：新增 7 個 widget stand-in(`THGemRadioGroup`/`THGemEdit`/`THGemCheckBox`/`THGemComboBox`/`THGemPanel`/`THGemSpeedButton`/`THGemMemo`)，翻譯 `InitialHGem`/`SaveSystemDefault`/`DoUpdateStatus`/`ProcessShow`/`FormClose`/`ManualCreatergRoleClick`/`FormShow`；`ht9045_secsgem` 首次連結 `ht9045_core`+`ht9045_globals`(確認無循環依賴)。**⚠️ KYEC 客戶部署前待辦**：`DoUpdateStatus` 的 30 秒強制斷線依賴的 `HTimer` 目前恆真(不會真的等 30 秒)，三處已清楚標註，尚未修。兩波皆獨立 fidelity-review clean、親自複驗 ctest 83/87(4 個既有無關環境漂移失敗)、mojibake 0。詳見 DEVLOG 2026-07-17 兩個區段。
- ▶ **下一步（不預設暫停，隨時可續，使用者 2026-07-11 已澄清暫停規則為情境性）**：**✅ W5-Final** + **✅ SecsWireCodec Wave 2** + **✅ 三前線同波(uHGemClass 10/57解鎖+AGV.cpp 剩餘全清+新 TServerSocket shim，2026-07-13)** + **✅ Automation/TesterTCP/uHGemEquipment 首建波(uHGemClass 13/57，2026-07-16/17)** + **✅ uHGemEquipment 連線生命週期 Bucket A+B(2026-07-17)**——見 DEVLOG 對應區段。**候選——`uHGemEquipment.cpp` C 桶**(`Timer1Timer`/`clientGemRead`/`ProcessSocketReceiveData`——本檔最終最大難塊，需先設計 `TCriticalSection`/`TMemoryStream` shim + `SecsWireCodec` 成員化)，或**真計時 `HTimer` 設計**(小而獨立，可解上方 KYEC ⚠️ 限制)。**W5 續**——`uHGemClass.cpp` 剩 44/57 個 gated method(多數卡 VCL widget/StringGrid-alarm/SV-EC 註冊家族，需逐個評估)、`BarcodeXML.cpp`(SendTestResultToHttp)、`uHGemHT9045.*` 站點覆寫層、`Interface/TesterTCP.cpp` 的 `TimerProcessTCPDataTimer`(真正協定解碼核心，需先設計 5 個 FormsFacade 成員)、`automation.cpp` 的 `ProcessBuffer`/`ProcessBuffer1`/`SendReportRequest` 派工核心(需先解 TServerSocket 消費端)、`SCK_ART.cpp` 剩餘(現與 `SCK_ART_Remainder.cpp` 有 3 方 state 協調債待解)。或 **W7 續**——csystem 剩餘 gated 大宗：MainProc mode/SECS/AGV/temp dispatch ladder(golden:16730-19101) + DoOneCycleFinishCheck/DoCleanOutFinishCheck 深層 seam(W7C1/W7C2，現 SECS_EVENT/AGV/SCKART/HANA 基礎已局部鬆動，值得重新評估) + DoAllProcess AutoRetest/Laser/AutoClean/OCR early-return ladder(golden:9165-10047) + atester T1 深層 tester-comm/cContact seam + 各引擎 W7-gated 整體 SM(需 MOT[]/sensor/encoder/fProductionInfo/cContact/SECS/DB/VCL form home)。或 **W6.2b** in/out site variants(25+26) pipeline(已 100% 完成，僅需 spot audit)。
- **W6.2 IN/OUT-ARM**：in-arm(幾何 2 模組+引擎 ainarm9045)+out-arm(引擎 aoutarm9045)已完成；剩 25+26 site variants(W6.2b) + W7-gated 整體 SM(需 MOT/sensor/prod home)。
- **W6.3 CATCHTRAY+FEED**：✅ acatchtray DoCatchTray + 5 子 SM 已完成；剩 asendic_Color/Loader… 其餘 feed canary 葉、fTrayMapping/Magazine/OCR/AMR/cassette 本體(W7+AMR)。
- **W6.4 INDEX/TESTER STAR**：✅ atester.cpp 引擎已完成(DoTestY/DoTestHeadMotor 核心 SM ACTIVE+pump、decode 消費者、5 oracle)。**W6.4b 進行中**：✅ Front/Rear「Destroy IC」對已真實翻譯(DoFrontTestDestroyIC/DoRearTestDestroyIC，Front↔Rear 互依不可拆但已一起落地)；✅ cContact `GetMinForce`/`GetMaxIndexForceLimit` 純算對已完成(extract-calc-core，見上方 2026-07-10 條目)；剩 atester_32Site/atester_ProcessCount + Front/Rear 其餘 export(DoFrontTestSuckIC/DoTestYFront 等，現 shim COMPLETE) + cContact 下壓叢集(`TfContact::DoTestContactFunction` 的 `CarlibrationTask`+6 sub-SM，非 `iIndexTask`，22761 行 VCL form 內 ~50%，純算對翻譯不減少此叢集範圍) + GetTesterResult/ProcessTestResult tester-comm 本體(W5)。
- **W6.5 SHUTTLE 生產 SM**：✅ acarry Do_Auto_SHT1/2/3 已完成(136/136 case ACTIVE、0 `#if 0`、5 oracle 過、Sim HAL pump)；VCL worker thread HThreadCtrlShuttle 做成薄離線 driver(SM 邏輯 verbatim)；剩 cross-module 本體走 acarry_shims(見 DEFERRED)。
- **W6.6 ORCHESTRATION HUB**：✅ csystem DoAllProcess/MainProc spine 已完成(672 行 active/~6% 方法、5 引擎以 A/B tick 序 pump over Sim HAL、34/34 ctest、4 oracle 過)；如計畫**只翻 spine 不翻整 25k 行**——gated 大宗(mode/SECS/AGV/temp ladder、finish-check 本體、early-return ladder)轉 W7(見 DEFERRED)。ckernel 仍待 W7。
**風險**：aArmHeader.h 分解(最高槓桿+風險)；csystem.cpp 必須拆；FormsFacade scope-creep(fMain 1000+ deref/~12 member 類)；SOFT_SIMULTE gating 下實機路徑未測；Front↔Rear 不可跨非相鄰子波(W6.4b Wave 1 已驗證此模式可行)；stale include 兩向誤導；cContact `CarlibrationTask`+6 sub-SM 下壓叢集(非 `iIndexTask`，該符號不存在，2026-07-06 recon 修正)；Big5 亂碼；acarry VCL thread。

## 延後項目追蹤（DEFERRED — 完整性，勿遺漏，全部轉移用）
> 部分檔案只翻了 leaf 部分，耦合段延到對應波次。最終各波結束前要回頭補完這些。
| 來源 | 延後的部分 | 目標波 | 原因 |
|------|-----------|--------|------|
| acatchtray.cpp / acatchtray_shims | fTrayMapping(63)/Magazine(59)/OCR(47)/uHGemHT9045 SECS/uteach 呼叫本體（現 offline shim 回 false/0/no-op）| W7 + W5 | TfTrayMapping/Magazine/OCR/SECS form/類別未翻 |
| acatchtray.cpp / acatchtray_shims | KYEC-AMR cover-tray + cassette SM 本體(DoCoverTray/CatchCoverTray/PlaceCoverTray/ReceiveCoverTray/DoLoadToCassette/DoCassetteToAuto, Eastsun F011/F016 + Frank cassette) + DoLoadCarRotArmReadRFID；SM skeleton ACTIVE，cross-module 本體 gated(fAGV->IsATK_AMR 離線 false 故 dormant) | AMR 波 | Magazine/HSys.BinDisCtrl/database/RFID home 未備 |
| acatchtray.cpp | 2× `#if 0` BinDisCtrl(FlashPro/ClearAutoChangingWarn) NULL-guard 呼叫 | W7-UI | HSys.BinDisCtrl(UI)未翻；離線 NULL 本就 dead，行為保留 |
| acatchtray.cpp | `WhichAutoNeedTray` 真本體已翻入但 MOT[]/Magazine/fAGV 內層 gated(離線回 0) | W7 | 需 Magazine/AMR home |
| ainarm9045.cpp | ~~25 個 `DoInArm_9045_<layout>()` + `*_SuckerMap()` per-variant 分派本體~~ **✅ W6.2b/c 全數完成**(batch1-5 全 ACTIVE，殘留的 2 個 `#if 0 TODO(W6.2b)` gate 內只剩空註解占位，無真程式碼) | ~~W6.2b~~ 已完成 | 25 in-arm variants 全落地(見 DEVLOG 2026-06-26 起 W6.2c 各 batch)；此列僅留存供追溯 |
| ainarm9045.cpp | 4 個整體 gated SM：`DoInArmPickFromLoadStage_9045`(中央取料,含 goto IN_ARM_LOADER+suck loop)、`DoInArmCheckShuttleFloating`、`CheckInShuttleSensor_Latch`、`DoInArmAutoCalSuckZ`（保 cursor+golden default，本體 #if 0）| W7 | 需 MOT[]/sensor/encoder/fProductionInfo home 才能端到端 pump |
| ainarm9045.cpp | `GetShuttleCol` per-iInArmType `XPHSuckToSht_*` 查表分支（ep1Picker early-return 活、default iShtCol=0）| W7 | variant 表陣列未定義 |
| ainarm9045 / aHotPlateSubstrate | `bInArmCheckDestroyACT`+4×`bPickFormHotplate*` 暫由 aHotPlateSubstrate.cpp 持有（避 ODR），所有權待還給 ainarm9045.cpp | W6.2b/整合時 | ainarm2/leaves 尚未 reconcile |
| aoutarm9045.cpp | ~~26 個 `DoOutArm_9045_<layout>()` + `DoPickFromShuttle_9045_<layout>()` per-variant 分派本體~~ **✅ W6.2b/c 全數完成**(27/27 檔：20 LIVE+6 DEAD+1 S-prefix DEAD，dispatch 100% live) | ~~W6.2b~~ 已完成 | 見 DEVLOG「OUT-ARM 變體 fan-out」B1-B5 記錄；此列僅留存供追溯 |
| aoutarm9045.cpp | 20 個 W7-gated 硬體/cross-module 本體（MOT[].ReadPos encoder、OutArmSuck destroy SM、Rotate/AOI/FixAI predicate+action、fNote/fAutoTeach VCL、MOT[MMPlate]）保 cursor+stub | W7 | 需 MOT[]/sensor/encoder/VCL form home |
| aoutarm9045.cpp | `EnableFix3UseCylinder`→false / `ReversionEmptyPoint`→no-op 暫由 aoutarm9045.cpp stub（cprod.h 有宣告但 cprod.cpp 本體在 #if0 W6 區）| W6（cprod ungate 時）| cprod.cpp 對應本體尚未 ungate；ungate 後移除這兩個 stub |
| atester.cpp | `GetTesterResult`(:849 ~1700 行 Tester GPIB/TCP 收發 + MOT/Suck/Socket bin 寫入)、`ProcessTestResult`(:2621 bin-decode+DB+UI)、`ProcessTesterTimeOut`(:3594) 整體 `#if 0`+active stub | W5(tester-comm) / W7 | Tester 通訊(GPIB/TCP)屬 W5；DB/UI 屬 W7 |
| atester.cpp (T1✅) | ✅ DoTestHeadMotor 下壓/test/torque/EP/load-cell case 樹(golden 6229-8650：case 30..600/12000../14000../122100../142../1500../10000../20000.. 多臂下壓 SM，107 case) 已 un-gate+忠實譯入(2026-07-06)，取代原 default-park stub；case 樹本身 ACTIVE，但其內部呼叫的深層 cross-module 本體（見下 2 列）仍走既有 atester_shims 離線 stand-in | 各對應子系統波次 | case 樹 skeleton 已 active；torque(COM2)/EP-DAQ/RTC-vision/cContact-ROI 等 leaf 本體未翻 |
| atester.cpp | cContact 下壓 SM 交握(DoTestHeadMotor case 樹內經由 gated leaf 呼叫觸及)；cContact.cpp 是 22761 行 VCL form 整檔未翻。**2026-07-06 recon 修正**：MIGRATION_ROADMAP 先前誤植的符號名「iIndexTask@cContact」不存在——真正下壓 cursor 是 `TfContact::DoTestContactFunction`(cpp:11755-13963)內的 `CarlibrationTask`(cContact.h:556)，加 6 個私有 sub-SM(`DoZ1/Z2PickFromShuttle`/`Do_Z1/Z2_AutoGetHeight`/`Do_ContactTest_32Site`/`DoZPlaceToShuttle`)，共~11347行≈整檔 50%；VCL widget 存取穿插 switch-case 內部(比 atester.cpp T1 更破碎，需比照更細碎的逐處 gate) | W7 | cContact TfContact VCL form 未翻；T1 un-gate 後 case 樹本身 active，仍不直接 deref CarlibrationTask(僅透過既有 gated leaf) |
| atester.cpp | DoCheckSocketHasIC/DoInterFaceErrorStep/CheckIndexStatus/CheckPlaceOutShuttle/DoSetupTest/GetTempICResult/CheckAndRecodrEP/CheckAndRecodrTorque/NewCheckAndRecodeTorque/IndexCheckOneByOne/IndexCheck4Site/CheckSocketSensor/DoIndexSocketCheck/DoHalfView*VerifyRTC/SendSiteMapToRTC/DoTesterSidePush/DoAllPassVerifyRTC/DoTestHeadMotorLoadCell/SaveTorque/CheckInitialStartDelayInSocket/.. 本體 `#if 0`+active stub(保 cursor+golden default)——T1 un-gate 的 case 樹是這些 leaf 的呼叫端，呼叫端已 active、leaf 本體仍 gated | W7 | MOT[]/Sen[]/Socket/EP DAQ/COM2-RTC/torque/VCL 本體需 home |
| atester_shims.{h,cpp} | atester_32Site(4749 行)/atester_ProcessCount(2161 行)/aTester_Front/aTester_Rear sibling SM(Do* 現回 COMPLETE/true、Init* no-op、cursor=1)；fContact(fShow=false/Do_ROILearning=true)；ADAM_* EP DAQ no-op；COM2/CCDInterfaceForm/fAutomation/fObserver/fiosetview/IsNNMode/CheckHeaterOK/EPSwitchOnOff/iCASE_REAL_CCD2 離線 stand-in | W6.4b + W5/W7 | sibling 檔未翻(Front↔Rear 互依)；rs232/CCDInterface/automation/cObserver/iosetview/cinitial form/class 未翻 |
| atester.cpp / FormsFacade | `fMain->LightOn()`/`lbCCDStatus->Visible`(CCD 燈/狀態 label)→FormsFacade no-op stub | W7-UI | main.h TfMain VCL form 未翻 |
| atester.cpp / canary_support | LastSet.iD47SocketTestedCount / bUseTestSocket[2][4][8] 加入 LAST_GENERAL_SET 最小 shim | W6.x | 完整 LastSet.h(~450 欄)尚未翻 |
| acarry.cpp / acarry_shims | `Tech`(6 偵測位 int=0)/`fLtcSensor`(latch count=0,GetLtc 回 0)/`OutSht3Kit`/`ATC_InterfaceForm`+`ATC_Data`(iATC_MODE_TYPE!=36→DoFloodGateClose false)離線本體；W7 換真 LastSet.h(TECH)/LtcSensor.h(TfLtcSensor)/ATC_Handler_Side.h | W7 | TECH/TfLtcSensor/ATC form/類別未翻；acarry.cpp 內 case 本體全 ACTIVE，僅這些外部物件走 shim |
| acarry.cpp / acarry_shims | `CheckShtFloating`/`UseIn/OutArmCheckShtFloating`(回 false)、`SortSHTInLF/RT`+`Check32site*`(回 false)、`DoStepShuttleCheck`(true)、`DoCheckShuttle1/2EmptyIC/MustHasIC/Prominent`(true)、`MoveOutArmXY_ToShuttleAlarmArea`(true)、`SendHTTPRequest`(0=OK)、`MoveSortArmToAutoSafe`(no-op) 離線 cross-module 本體；call site 在 acarry.cpp ACTIVE 但決策物件離線回安全值 | W7 + 9046AU/HTTP 波 | csystem.cpp/LaserSensorShuttle.h/9046AU sort-arm/HTTP server 未翻；離線取 canary 安全分支 |
| acarry.cpp / acarry_shims | `MySleep`/`MySleepEx`(no-op timing；common.cpp 本體 `#if 0` TODO(wave-timing))、`OutShuttleLog`(cpublic.cpp `#if 0`，需 fMain UI)、`LogSoftwareOffTime`(main.cpp)、`MoveOutArmToAutoSafe`(aoutarm.cpp 真本體未翻，目前離線回 true)、`SystemNG`/`iCloseSiteStep_2x8`/`bCarryControlOutarm1/2`/`bPickShuttleError`(aoutarm.cpp/main 全域，離線初值 false/0) 由 acarry_shims 持有 | W5/W6/W7（對應子系統翻譯時還原）| common.cpp 計時/log 區 ungate、aoutarm.cpp(carry-control 全域+MoveOutArmToAutoSafe)未翻、main 側 SystemNG/log；`IsNNMode`/`SetMotorScaleSpeed` 為 forward-decl(真本體已在 sibling shim) |
| acarry.cpp uShuttleThread | `HThreadCtrlShuttle::ThreadProcess` + `Initial/Start/EndShuttleThread`/`StartDetectMotorSensor` 的 Win32 worker thread wrapper做成薄離線 driver(SM 感測輪詢邏輯 verbatim、無真 OS thread)；MySleepEx/HANDLE/DWORD token 走 acarry_shims | W7 | 真 thread 生命週期+fMain meShuttle1/2 UI log 需 home；離線 SM 邏輯本身已可單步 pump |
| Motor/mymotor.cpp | Gali_Two_ZAxis_Move / GalilTwoY_Move / ISNormal 改 Motor==NULL 快路徑(離線回 true/in-position)；real Galil 本體仍 `#if 0`(W4-part2/W6-Galil) | W4-part2 / W6-Galil | 真 Galil 移動/狀態需 vendor SDK + index home |
| csystem.cpp | MainProc 完整 mode/SECS/AGV/temp dispatch ladder(golden:16730-19101 ~2000+ 行：ScanSystemSensor/DoSystem/DoTriTempState/DoHomeProcess/DoTrayFeedProcess + UI/comms/DB 本體)整塊 `#if 0`，現 MainProc 薄 head 直接 dispatch spine(golden:18728-18740 inner-most Run-path) | W7 + W5(SECS) | 那些 TU 未翻；spine 是 Run path 最內層、薄 dispatch 已足以讓週期 pump |
| csystem.cpp | DoAllProcess AutoRetest/Laser/InitialICCheck/Magazine/Cylinder/LoadCell/ATCTemp/Decay/AutoClean/EmptySocketCheck/OCR/GPIB-wait/UTAC-wait/ClearSocket early-return ladder(golden:9165-10047)整塊 `#if 0`(含 golden:9681 AutoClean SOFT_SIMULTE 區)；正常 Run path(`flag=true` golden:9473)以下 ACTIVE | W7 + W5 | 觸及未翻 UI/OCR/DB/SECS surface(fContact->IsRun2DCheck/ShowMyMessage/fOCR/fLotInfo)；AutoClean 引擎(DoAutoCleanKit/DoShuttle1/2AutoClean/DoIndexAutoClean)存在但整塊隨其 UI guard 一起 gate |
| csystem.cpp | InitCleanOutFunction 的 rsmAutoSiteMap 分支(golden:15751-15785：HotPlateForm/fMain SetMainRunStartMode/MMPlate SiteMapData grid)`#if 0`；簡單 iHome/iReset/iCleanOut/iTrayFeed/bCleanoutStart cursor 重置 ACTIVE | W7 | HotPlateForm/fMain mode setter/MMPlate site grid 未翻 |
| csystem.cpp | DoHotplateEdgeCylinderLoop / DoLoaderVibrateLoop 本體(Cylinder[] edge-push 陣列+timing / MOT[MLoader*] vibrate)`#if 0`+active no-op 本體；spine tail 照 link+pump | W7 | Cylinder/MOT vibrate timing 需 home |
| csystem.cpp (C1✅) | ✅ DoCleanOutFinishCheck(golden:14713-15748 ~1035 行) 真本體已譯(csystem.cpp:1103；core drain active、真-drain oracle 7/7 驗、ODR 單一 def)；剩深層 config-gated customer/mode 分支經 **155 W7C1_ TU-local seam** 接(離線 inert)：SCKART AutoRetest 欄位/方法→Automation/SCK_ART、fOCR/fContact/fTrayMapping/fBarCode form→W7-UI、fAGV IsSPIL_AMR/AMR tray-count→AMR、SECS_EVENT.CleanOutFinish/AGVSupplement→W5、QA/Low-Yield/tray-feed finish ladder、LastSet 4 欄、free func/global/object stub | 各對應子系統波次 | 對應 form/SECS/Automation/AMR/LastSet 未翻；core drain 已 active |
| csystem.cpp (C2✅) | ✅ DoOneCycleFinishCheck(golden:12813-14047 ~1235 行)+DoART_AfterCleanOut(golden:14049-14711 ~663 行) 真本體已譯(csystem.cpp:2367/3626；One-Cycle finish + ART drain core active、真 oracle 7/7 驗:播種殘料→cursor 5 不 false-finish、ODR 單一 T×2、no-op stub 移除)；剩深層 config-gated customer/mode 分支經 **30 W7C2_ TU-local seam** 接(離線 inert)：SCKART AutoRetest 全套→Automation/SCK_ART、RENESAS_Server→uRENESAS、hanaART→uHANA_ART、SECS_EVENT.OneCycleFinish/ArtFT/ArtRTFinish→W5、fSocketCommunication(ASE_KR)/fHome/fAutoTeach/Low-Yield/Magazine/QA form→W7-UI、LastSet/LotSummary 擴充欄 | 各對應子系統波次 | 對應 form/SECS/Automation/RENESAS/HANA/LastSet 未翻；core finish/drain 已 active |
| csystem_shims.cpp | DoInArm/DoOutArm 之外的 cross-module 葉(DoLoad@asendic_Loader、DoSortArm@asortarm 9046AU、InitLoadTask/InitOutArmTask/InitialDoLockUnloader/Initial_Auto_BinTray_Task/InitAutoColorTask/4×Magazine init、DoAuto3Magazine/NewDoAutoTrayEdgeCylinderLoop/DoAuto2、DoInArm/OutArmTeachAlignmentProcess 回 true、SetFixTrayMiddleDtata) ACTIVE 離線 no-op/cursor-reset/回 true | W7 + AMR/9046AU 波 | asendic_Loader/asortarm/Magazine SM/AutoAlignment/aoutarm Fix-tray 本體未翻；DoInArm/DoOutArm 已真路由到 DoInArm_9045/DoOutArm_9045 引擎 |
| csystem.cpp / aHotPlateSubstrate | DoOutArm golden:1197-1209 kit-prep(FRCarryKit/BRCarryKit.SetHasNullIcToNullIc + 結果-tag pass) skip——substrate 的 TMyKitSuck 尚未暴露 SetHasNullIcToNullIc(只有 SetAllToNullIC)；離線 carry kit 起始空故本就 no-op | W7 | 需把 SetHasNullIcToNullIc 加到 TMyKitSuck 後還原 verbatim prep loop |
| csystem.cpp / iAllArmZHomeTask | `iAllArmZHomeTask`(InitAllProcessTask:5718 設=1)的 consumer `DoArmZHome` 整體 SM 未翻 | W7 | Arm-Z-home FSM 需 MOT[] Z 軸 home |
| Public/ExternFunction | StringGrid_Insert_Row/Delete_Row、StatusBar_ItemText、ShowRecordTime | W7 | VCL TStringGrid/TStatusBar/TEdit |
| Public/ExternFunction | DeleteDirectory | W3 | 需 SysUtils FindFirst/FindNext/TSearchRec shim（未在 vclcompat） |
| Public/WinSocketErrorCode | LogClientSocketExceptionError | W5 | 耦合 VCL TClientSocket(Name/Address/Port)+MyDBIProcess |
| Public/MyStringList | 整檔 | W3 | 耦合 IniConfig/RunInfo/LastSet/CUSTOMER_CODE、VCL 表單、Win32 file |
| Public/MyProductionRecord | 整檔 | W6 | Prod/IniConfig/RunInfo/CUSTOMER_CODE、state-machine 消費端 |
| Public/HonNewDel | 整檔 | W7 | template 依賴 MemoryAlarmForm（VCL form）|
| Public/MemoryAlarm | 整檔（本身是 TForm）| W7 | VCL 表單 |
| Public/HTEdit, HTEditList | 整檔 | W7 | VCL 編輯控制項 |
| vclcompat 待補 | SysUtils FindFirst/FindNext/TSearchRec、faAnyFile/faDirectory | W3 前 | DeleteDirectory 等檔案搜尋需要 |
| cUnitConvert | Do*Convert glue (DoTestIFConvert/DoDeviceConvert/DoHotPlateConvert/DoArmOffsetConvert/…) | W6 | 依 cprod.h/cmydef.h 結構 + fShowMessage/ATC VCL form |
| SortingBinTray | 整檔 | W6 | main.h/MyMotor/mycylin/atester/uLotInfo + cmydef TSortingBinTray_* + AutoForm[] VCL form |
| Common/PickPlanner adapters | cInArmLoaderPickPlanner/cOutArmPlacePlanner 的 Search 本體（現為 scaffold，gate 預設 false）| W6 | 綁 MOT[].Tray.Data / InArmSuck / OutArmSuck（未翻全域）|
| cmydef.cpp / cprod.cpp / cpublic.cpp | 觸及 main.h/csystem/rs232/MyMotor/state machine 的函式 body（現 `#if 0`）| W3/W5/W6 | 等對應模組翻完 |
| cmydef.cpp | `TMyLog myLog` 全域實例定義（header 已前置宣告）| W7 | TMyLog 綁 VCL form/TWinControl |
| cmydef.cpp | 5 個 `TMyStringList*` 目標定義 | W3 | TMyStringList 在 Public/MyStringList.h(__property+MyMemo.h) |
| MachineDefine.h | driver/app include 區塊 | W3/W6/W7 | 等驅動/app 模組 |
| database.cpp | ReadGeneralIni(~1240行)、SystemModularInitial(SECS HT9045Gem)、TDataModule1(BDE)、InstallColorBinDisplay | W5/W6/W7 | 牽連 SECS/BDE/UI；本批只翻 config-table loaders |
| cpublic.cpp | queue/union/VerInfo 方法本體（現整檔 #if 0）| W6/W7 | cmydef.cpp 的 active queue 全域(QueueTaskList[]…)需其 ctor；**任何用到 queue 的 exe 連結前必須先 ungate**（目前測試用 stub ctor 繞過）|
| Motor 品牌驅動 ×6 (mySMCmotor/myGALILmotor/myMN200motor/mySYNTEKmotor/myEthercatmotor/HTMC88X1Motor) | 整檔 | W4-part2(需 W6) | 各 .cpp 拉 main.h/csystem/sensors/cylinders god-stack（卡 W6）+ vendor 呼叫需 #if HAVE_CSMC/GALIL/MN200/SYNTEK/ETHERCAT/MC88X1(預設 OFF，無 SDK)。vendor 符號族見 W4 workflow 紀錄 |
| Motor/mymotor.cpp | MotorMovePosition/MotorMove/Galil 分支(~80)/sensor compare/TrayArm/continuous-move 本體（gated）| W6 | 拉 sensor/cylinder/狀態機；pHTray(VCL)→W7 |
| mycylin (TMyCylinder) | Push/Pop/On/Off/SetSimulateCompoment 狀態機（IO 路由 method 已翻）| W6 | 拉 Alarm/SystemStart/bHandlerPause/SmartDiagnostic/VCL TControl |
| MyKitSuck | TMySucker Suck()/Destroy() 狀態機；**TMyKitSuck 整個 item-state grid（2894 行）** | W6 | TQPF_Timer/alarm；TMyKitSuck 拉 TALed/TMyProductionRecord/TTrayMotor/MyMotor/cprod IC 常數（god-stack，非 IO-HAL）|
| myio.cpp | raw-port free funcs（outportb/IOSetOutport/TTL_CARD_TYPE）| W6/x86-dead | 本機 TTL_CARD_TYPE>0 短路、ISABase 全 0→實際不走；x64 無 |
| cprod.cpp(283)/cpublic.cpp(62) 中文**註解**亂碼(U+FFFD) | 低優先 cosmetic transcode | 任意(收尾) | 確認 100% 在 `//` 註解、無 string-literal 受損(非功能)；golden 有原文。修法：from golden 以 cp950→UTF-8 重寫註解，或英文 gloss+golden ref。go-forward 規則已防再發 |
| SECSGEM/uHGemClass.cpp | 剩 **44** 個(原 57)virtual method 本體(現 `#if 0`，各自標註具體缺口非空泛「等 THGem」) | W5-SECSGEM-engine | **2026-07-13 解鎖 10/57**(`HTGem` 內嵌 by-value `SecsWireCodec WireCodec;`+`SecsSvEcRegistration SvEcReg;`)。**2026-07-16 Unlock2 再解 3 個(13/57)**：`GetDataItemLenAndTypeAndDelete`+`SendInvalidDataMessageToHost` 已翻入 `SecsWireCodec`，解鎖 `S7F2`/`S2F15_Update`/`S2F15_Check`——原「~17-method 最大共同缺口」cluster 剩 14 個仍卡其他缺口(S7F18/S7F20/S10F4/S10F6 需 VCL widget、S2F13~S2F38 需 SV/EC 註冊 DB 家族、S125F2 需 StringGrid-backed EnableDisableECData)。其餘缺口分類不變：VCL StringGrid 資料庫(5 個)、VCL widget 型別(5-6 個，含 `SetECValue` 的 TPanel/TComboBox cast 叢集)、THGem-only 資料成員(~12)、THGem-only 方法(~18)、Borland `dos.h`(1)。詳見 DEVLOG 2026-07-13/2026-07-17 區段 |
| SECSGEM/uHGemEquipment.cpp | 剩餘（**C 桶**）：`Timer1Timer` 本體(golden :5176-5527 mega SM)+`clientGemRead`/`ProcessSocketReceiveData`(真正協定解碼核心)+`DoConnect`/`DoSelect`/`DoSeparate`(需 `SecsWireCodec` 成員化)+`DoProcessSFNoResponse`+`DoLocalAllProcessLoop` 五子系統(spool/trace/upload/download)+`FormCreate` SV 註冊(需 `SecsSvEcRegistration` 成員化)；另有 **KYEC 真計時 `HTimer` 待辦**(現用恆真 stub，30 秒強制斷線不會真等) | W5-SECSGEM-engine-C | 需先設計 `TCriticalSection`/`TMemoryStream` shim(全樹尚無)。**已完成累計**：2026-07-11 `SecsWireCodec`+`SecsSvEcRegistration` 前置切片；2026-07-13 Wave 2 訊框 header+SML 解碼器 15 函式；**2026-07-16 `THGem` 首建**(headless `vclcompat/StringGrid` shim+CEID/Report/Alarm StringGrid 資料庫家族 26 method)；**2026-07-17 連線生命週期 A+B 兩桶**(clientGem/srvGem 開通 28 函式+7 個 widget stand-in+`InitialHGem`/`SaveSystemDefault`/`DoUpdateStatus`，`ServerSocket.h` shim 首個真消費端)。`uHGemClass.cpp` 整合進度另見上列(13/57) |
| SECSGEM/uHGemHT9045.{h,cpp}+_SV.cpp+_EC.cpp | 整組(~9200行)，站點覆寫層(command/SV/EC override，SECS_EVENT 消費端) | W5-SECSGEM-override | 全樹最寬表單 fan-out(~22 個未翻表單：fLotInfo/fSCKART/fBinSel/fNote/fSetup/fGroundMan/fSmartDiagnostic/fContact/...)，需待這些表單各自有 home 才能安全翻 |
| SECSGEM.cpp | 整檔(2052行) | **不翻譯** | 2026-07-11 recon+主迴圈核實：死碼，`SECSGEM.h` 全樹不存在、`HT9045.bpr` FILELIST 未列此檔，`SECS_GEM` 巨集從未被 `#define` |
| Interface/TesterTCP.cpp | 剩餘：**`TimerProcessTCPDataTimer`(真正協定解碼核心**，需 5 個新 TfMain FormsFacade 成員+未翻譯的 `WritePERSITETemperature` 姊妹函式)、ctor/FormDestroy/FormShow/`rgUnloaderClick`(~140 行 widget 建構群)、`CopyOSTestResult`/`PlaceOSTestResultToTray`(卡 `Public/MyProductionRecord.cpp` 的 `GetSiteNo`/`GetOrderOfContact` 缺口)、`SimulateBin`/`ProcessOSPrint`/`ProcessOSTrayData`(TRichEdit/TMemo 密集) | W5-TesterTCP | golden 1117 行。**2026-07-11 交付第一子集** `TesterTCP_Socket.cpp`(socket 管理層)；**2026-07-16 交付第二子集** `TesterTCP.cpp`(OS-recipe zip 收發對 `CopyRecipeToTester`/`CopyRecipeFromTester`+`btnSaveClick`，golden :1058-1115，與第一子集零重疊) |
| CCLink/MyCCLinkSensor.cpp | 表單殘餘(~2560行，含一個 672 行即時刷新迴圈)；本波僅抽出 2 個純 predicate(`UseCanBusOrEtherCAT`/`UseSensorMod3`)到獨立檔 `MyCCLinkSensor_predicates.cpp` | W7-UI + CCLink 專屬波 | `TfCCLink` VCL 表單本體，2026-07-11 recon 確認在此機台仍會**無條件在啟動時被實例化**(即使 CC-Link 功能設定關閉)，需審慎處理 |
| CanBus/cMyDNM100UD.cpp | 整檔 | 待驗證後排入 | recon 發現一處疑似建構子 `Owner` 參數缺陷，需先獨立驗證釐清才能安全翻譯，本波刻意跳過(僅翻兄弟檔 cMyNUDN1.cpp) |
| EtherCAT/MyEtherCAT.cpp | 整檔，真廠商呼叫本體 | W4-part2(需與 Motor EtherCAT 軸驅動聯合決策) | 與另一個延後的 `myEthercatmotor.cpp`(Motor 軸驅動)共享全域 `uiDevhand`+重新初始化 `MOT[]` 副作用，兩者需一起決策，非單純 W5 內部事務 |
| BarCode/{BarCode,BarCode_Sh1,BarCode_Sh2}.cpp 剩餘部分 | recon「Category C」相關函式(`DoSFCAutoTune_1/2`/`Do2DIDCheckSh1/2`/`CCDBarcode_Decoding`系列，非 shim 方法但同域低風險) | W5-BarCode 續 | `TfBarCode_Shim` 20/20 方法本身已於 2026-07-11 W5-Final 全數交付(`BarCode_Shuttle2_ScanRemainder1/2.cpp`)，此列僅剩 Category C 非-shim 函式 |
| BarCode/BarcodeXML.cpp | 整檔(1372行，`TfrmXml`，含 `SendTestResultToHttp` 1067 行本身即地雷) | W5-BarCode 續(需 TClientSocket HAL 波 + Indy TIdHTTP/TXMLDocument 替代方案) | 與 acarry_shims 既有 DEFERRED 的 `SendHTTPRequest(0=OK)` stub 直接相關 |
| BarCode/BarCode.cpp/BarCode_Sh1.cpp/BarCode_Sh2.cpp 的 `__published` widget 區塊 + 純 UI glue(~3467行) | Settings 分頁 UI(bin-mapping combo box、IP/port 欄位、手動測試按鈕) | W7-UI | 純設定介面，抽取無價值，整段延到 UI 波次 |
| Automation/automation.cpp | 剩餘：`ProcessBuffer`(golden :951-1979，~130-branch OLP 命令派工 ladder)+`ProcessBuffer1`(:1980-2045)+`SendReportRequest`(:2046-2131)——合計 ~1181 行 ≈ golden 47%，是 OLP 引擎真正核心，現為 3 個明確標註的空殼 stub；另 NMUDP1/Amkor-UDP 6 方法+虛擬鍵盤 handler 整組未宣告(無消費端，無限期延後) | W5-Automation 續 | golden 實為 2501+153 行(先前「~5500行」係誤植)。`auto9045.cpp`(148 自由函式)2026-07-11 交付；**2026-07-16 交付網路層**(41 真譯 method+6 自由函式：ctor 客戶碼分支/event API/`SendClient`/`SendServer`/`OLPServer`/`OLPClient` handler/`tmrOLPTimer`/JSON 匯出，單例名 `fAutomationEngine` 避 ODR)。`ProcessBuffer` 派工核心建議與 `uHGemEquipment` C 桶同期(共用 TServerSocket 消費端經驗) |
| Automation/AGV.cpp | 僅剩明確延後的 VCL glue(ini 編輯器六件套`spbSaveClick`等、`Timer2Timer`需新 `TfNote` facade、`ShowE84Sensor`36-widget ALed 陣列、`CheckSECSValue`golden 本身空、3 個 `MouseDown` fQwertyKey 彈窗) | W7-UI | **✅ 2026-07-13 三前線波已交付剩餘 13 個可翻函式**(`AGV_PortScan.cpp`：9 個零依賴+4 個需 3 新 FormsFacade 成員)，加上先前 `AGV_E84.cpp`(E84握手)+`AGV_predicates.cpp`(3 predicate)——全檔狀態機邏輯已清空，只剩 VCL 表單本體待 W7 |
| Automation/SCK_ART.cpp | 剩餘部分(扣掉本波 8+8 共 16 函式) | W5-Automation 續 | VCL widget 密集片段，部分卡在尚未翻譯的 `LastSet.h` 欄位。**2026-07-11 W5-Final `SCK_ART_Remainder.cpp` 再交付 8 函式**(`SetSetupFilePath`/`AccessFile`/`SetGPIBVersion`/`UpdateCount`/`AddAlarmCode`/`ClearLotInfo`/`AddOutputJamCnt`/`SaveTestSummary`-dispatcher)，但**刻意自帶獨立 `SckArtRemainderState`**(非姊妹檔 `SckArtState`)——遺留 3 方 state 協調債(`SckArtState`/`SckArtRemainderState`/`csystem.cpp` 的 `W7C2_TfSCKARTSeam`，皆各自持有 `dCurrYield`/`iFTRTCount`/`iNeedRT`等同名欄位副本)，未來整併波需一併解決 |
| MR/(全目錄，acatchcassette.cpp/RFID.cpp/Cassette.cpp/Delta.cpp 等) | 整目錄 | **不排入任何未來波次** | 2026-07-11 recon+主迴圈核實：`HT9045.bpr` FILELIST 完全不含 `MR\` 前綴任何檔案，全目錄死碼；除非未來有明確客戶需求，否則不翻譯 |
