// ============================================================================
// DO NOT EDIT -- machine-generated by emit_rc.py W7-B1b v1.0 on PMAlarm/PMAlarmShowData.dfm
// AI(W906-W7-B1b) 20260728: generated .rc/_ids.h from the .dfm IR (see
// docs/W7_UI_ARCHITECTURE_PLAN.md S6 W7-B1b). Hand edits will be
// overwritten and are rejected by the idempotence gate (G7).
// Source golden: PMAlarm/PMAlarmShowData.dfm
// ============================================================================

#include <windows.h>
#ifndef DS_CONTROL
#define DS_CONTROL 0x0400L  // afxres.h is out of reach at this wave (see plan D8/G10 note);
                            // MinGW windres pre-defines this via winuser.h, MSVC rc.exe does not
#endif
#ifndef SS_REALSIZECONTROL
#define SS_REALSIZECONTROL 0x00000040L  // MinGW winuser.h lacks this (verified: absent from
                                        // C:/MinGW/include/winuser.h) while the Windows SDK
                                        // winuser.h rc.exe uses has it -- without this guard
                                        // windres chokes with a bare "syntax error" on every
                                        // TImage/TEBW8Image control (S9-R2: windres is the
                                        // secondary runner, not authoritative, but G5 requires
                                        // both to pass when both are present)
#endif
#include "PMAlarmShowData_ids.h"

IDD_PMALARMSHOWDATA DIALOGEX 0, 0, 978, 625
STYLE DS_SETFONT | WS_POPUP | WS_CAPTION | WS_SYSMENU | WS_CLIPCHILDREN
EXSTYLE WS_EX_CONTROLPARENT
CAPTION L"PM Alarm Show Data"
FONT 8, L"MS Sans Serif"
BEGIN
    CONTROL L"", IDC_PMALARMSHOWDATA_SG_PMALARMSHOWDATA, "HT9045.StringGrid", 0, 8, 72, 945, 505
    CONTROL L"cb_PMAlarmChoiseFile", IDC_PMALARMSHOWDATA_CB_PMALARMCHOISEFILE, "ComboBox", CBS_DROPDOWNLIST | WS_VSCROLL, 208, 32, 337, 28
    CONTROL L"Choise Report File", IDC_PMALARMSHOWDATA_PNLPMALARMBEFOREDAYS, "Static", 0, 32, 32, 169, 28
    CONTROL L"Report View", IDC_PMALARMSHOWDATA_PNLPMALARM_TITLE, "Static", 0, 0, 0, 970, 28
END

