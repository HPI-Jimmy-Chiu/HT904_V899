# MG_FINAL_DECISIONS — 最終決策清單（留使用者裁決）

依使用者 20260826 裁定：<95% 信心的項目記在這裡，戰役收尾一次裁決，過程不停。
每條含：叢集／衝突描述／選項／我的建議／若已依 95% 規則先行的動作。

| # | 叢集/案 | 問題 | 選項 | 建議與先行動作 |
|---|---|---|---|---|
| F1 | Multi EP（0430/0504/0511/0526；欣銓 CASE-20260429/0512） | ~~搬不搬 8ch~~ **已被地形解決（MG-W7 實測）**：V910 有公司自製 Multi EP 完整實作（Eastsun 20260525 整合＋RogerYang 8EP 等後續演進，部分比 V899 新）。真缺口僅 6 條（5 條 iosetview AV 防護＋1 條 ContactForce round-trip）已搬；其餘 C 類/公司新功能不可刪，入白名單 | — | **無需裁決**（原問題不存在）；殘餘個案拆成 F3–F6 |
| F3 | adam6024.cpp 0526 兩塊（V899:1627/1670） | **V910 有反向刻意修改**：Eastsun 20260616 把這兩塊逐行註解掉、改用迴圈內版本 | (a) 尊重 V910 不搬 (b) 回搬 V899 版 | **建議 (a) 不搬**（公司刻意改），已照建議執行（未搬） |
| F5 | ContactForce.cpp:453（0526，**閘外**） | 照搬會讓 INDIVIAL 機台既有 `Diameter_20.000mm_*` 校正群組全部孤兒化回預設 | (a) 不搬 (b) 只在 MULTI 模式鏡射（需另開實作波） | **建議 (a) 不搬**；若欣銓日後真開 MULTI 再議 (b)，已照 (a) 執行 |
| F6 | ContactForce.cpp:884（0430，閘外、純外觀） | INDIVIAL 時 V910 多顯示一個空分頁；搬移需覆寫 Eastsun 區塊，信心 90% | (a) 不搬 (b) 搬 | **建議 (a) 不搬**（外觀差異 vs 覆寫公司區塊的風險不成比例），已照建議執行 |
| F7 | MG-W9 發現：V910 的 `FUNC_CC_ARDENTEC()` 比 V899 少三行（bTrayDeviceCheckFromLoader／bTrayOCR／bFTPFunction，皆非 899 期矩陣範圍） | 後果：本波搬入的 acatchtray case 148 修正在 V910 的**欣銓機台跑不到**（IsRunDeviceRemainLaser 需 bTrayDeviceCheckFromLoader==true；其他有開此 profile 的客戶會生效） | (a) 維持只搬矩陣範圍（現狀） (b) 把三行也補進 V910（超出 899 期範圍） | **建議 (a)**；若欣銓在 V910 機台需要此功能，再以獨立需求補 (b)，已照 (a) 執行 |
| F8 | MG-W9 acatchtray 3 條為**閘外變更**（V899 最終態本就無閘） | 新增一次 MLoaderY MotorMove＋三顆氣缸釋放（D 類動作），但需四道獨立閘全開才會走到（USE_TRAY_MAPPING==etmDeviceRemain＋客戶 profile＋UI 勾選＋INSTALL_OCR_YMot） | (a) 忠實照搬（已執行） (b) 加閘（=新寫碼，違反 splice 鐵律） | **建議 (a)**，已照建議執行；blast radius 已量化記錄於 mg_w9_analysis.md |
| F9 | watchdog 本體的掛鐘缺陷隨波帶進 V910（TQPF_Timer 純 QPC 無 stop 感知；WAR09102/09107 訊息分家未做）＝V899 已知缺陷 D5 | 忠實搬運原則 vs 已知缺陷擴散 | (a) 忠實搬運（已執行），要修另開 V899 案兩樹同步 (b) 搬移時順手修（違反忠實原則） | **建議 (a)**，已照建議執行 |
| F10 | MG-W11 的 OP6b（AutoClean 0407「kit 無 pad 防卡死」，**信心 92% 故本波未搬**） | 搬入後：非 KYEC 修好「kit 全空時 Task 卡 10 永久卡站」；但 CC_KYEC_LEE 的 ShowMyMessage（會設停機）觸發條件由 count==0 反轉為 count!=0——「kit 只剩用過 pad」情境從安靜變跳停機彈窗 | (a) 忠實搬運（V899 出貨現狀，KYEC 行為變化=V899 已接受的行為） (b) 搬運＋KYEC 條件維持 V910 舊式（新寫碼） (c) 不搬（V910 保留卡站缺陷） | **建議 (a)**；已實測單獨補搬不影響其餘 16 op（splice op 在 docs/mg_w11_ops.json 的 OP6b，可單獨執行）。等你點頭後一個 op 收尾 |
| **F4 ✅ DONE** | ~~adam6024.cpp 0511 三條 bounds guard~~ | **獨立稽核推翻原理由**：原判「V910 預設值下越界不可達」為**錯誤**。實證：V910 `ContactForce.cpp:501-538` 的 push_back 全包在 `atof(...)>15.0` 過濾裡，recipe `[SLK Type Ind] Type` 只要含空欄或 ≤15.0 的值，該 i 不 push 但迴圈仍跑到 Count → `SLKIndClass.size() < Count*8` → `operator[]` 讀到野指標並解參考，餵進 `iAPAXEPValue[]`＝**實際 EP 氣壓輸出**。V910 三處索引點（`:1671`、`:1705/1712/1718/1719/1721`、`:1396-1401`）零 size 檢查，唯一的 size 變數是 Eastsun 20260616 註解掉的死碼；V899 的 `if(kMep<8)` 寫入夾限在 V910 也不存在（`iAPAXDualEPValue` 是 int[16]） | (a) 補搬防呆（需為 V910 迴圈結構改寫，非機械 splice） (b) 維持不動 | **20260901 已補搬（使用者裁決）**：V910 的 j<16 迴圈補上 `iSLKIndSize` 與 `if(i*16+j>=iSLKIndSize) break;`（模式閘門 V910 已有 Eastsun `continue` 保險，不重複加）。⚠F15 的 j<8 MULTI 迴圈**未動** |
| **F15 ✅ 部分 DONE（防呆已補／口徑未動）** | Multi EP 防呆與口徑對齊共 5 條——`adam6024.cpp` 0526 ×2、0511 ×2（見 F4）、`ContactForce.cpp:453` 0526 | **原判 F3–F6「尊重 V910 演進不搬」的前提被推翻**。三處實證都指向 **EP 氣壓輸出**：(1) `adam6024:1627-1668`／`:1670-1702`：V910 有 Eastsun 20260525 的等價計算，但**少了 V899 的 `size()` 防呆與 `if(kMep<8)` 寫入夾限**；`SLKIndClass` 是 `vector<T*>`，`operator[]` 不檢查 → 野指標解參考取 dDiameter/dLoadRate，垃圾值一旦碰巧 match 就寫進 `iAPAXEPValue[]` → `APAX_WriteData` 送到 APAX-5028 AO 通道（Index 下壓的實體氣壓）；且 `k` 無夾限時 `iAPAXEPValue[16..]` 會**寫穿到相鄰宣告的 `iAPAXDualEPValue[0..]`**（cmydef.cpp:5050/5051 相鄰、皆 int[16]）。(2) `ContactForce.cpp:453`：V910 的 `[SLK Type Ind] Type` 仍是 legacy `"20,30"` 且**全樹無回寫路徑**，裝 40/56/58mm kit 時恆不匹配 → 該 pass 完全不寫 `iAPAXEPValue[]`，而它是**開機歸零、計算前不清空的全域** → 冷機首跑外圈 EP 幾乎無壓、換 recipe 後沿用前一組 kit 的壓力值 | (a) 補搬防呆與口徑對齊（需為 V910 迴圈結構改寫，非機械 splice） (b) 維持不動 | **20260902 使用者裁決「先只修兩個防呆」，已執行**：(1) 越界防呆 `if(i*8+j>=size()) break;` 補進**單力（SLKIndClass）與雙力（DieForceOneByOneSLKClass）兩個區塊**；(2) 寫入夾限 `if(k<8)` 同樣補進兩區塊，包住既有 `bUseDieForce` if/else 不改其邏輯。**新查證**：`iAPAXEPValue[8]/[9]` 是 DualSite 測試模式的 EP 輸出（adam6024.cpp:2636-2637，走 `wData` 非 `wData2Mep`），故 k 未夾限時**在寫進隔壁陣列之前就會先污染 DualSite 的壓力值**——夾限 `<8` 是正確界線（與 V899 相同）。**⚠ 口徑對齊（ContactForce:453，`[SLK Type Ind]` 釘死 "20,30" 且全樹無回寫）依裁決未動**，其後果（冷機首跑外圈 EP 無壓／換 recipe 沿用前組壓力）維持現狀 |
| **F16** | `ContactForce.cpp:884` 0430（原 F6，判「純外觀不搬」） | 稽核維持 FALSE 但**確認為低嚴重度**：V910 把分頁顯示邏輯包在 `if(bUseDynamicKitDiameter)` 內，`false` 時從不指派 `tsDieForceOneByOneKit->TabVisible`，dfm 預設 True → 操作員看到一頁空白的 DieForce One By One。不影響運動控制／氣壓／警報／存檔 | (a) 補搬 fallback (b) 不搬 | **建議 (a)**（成本極低），但可與 F15 分開處理 |
| **F11（第二輪推翻，需重裁）** | ~~acatchtray 0703 防抖兩條~~ | **原判「V910 有更演進版」對非 KYEC 客戶不成立**：V910 條件是 `(CUSTOMER_CODE!=CC_KYEC_LEE \|\| iCnt>iTrayDetectCount) && bCoverTrayInProgress==false`——非 KYEC 時第一個 OR 恆 true 短路，**防抖從未生效**；且計數重置只在 `else if(CUSTOMER_CODE==CC_KYEC_LEE)` 內，非 KYEC 永不清零。決定性反證：V910 自己的姊妹路徑 `DoPlaceTrayToAuto_250`（:4141-4171）**保留了客戶無關的 `iCnt>100`**，與 case 570 自相矛盾 | (a) 補回非 KYEC 的防抖（需合併兩版語意，非機械 splice） (b) 維持 | **建議 (a)**——中嚴重度：TrayArm 夾爪 sensor 單次瞬時抖動會讓非 KYEC 機台誤報 JAM0610，正是 V899 該修正要消除的 |
| **F13 ✅ DONE** | uTrayEditForm.cpp 0519 兩條（MG-W16 原判 C 類） | **獨立稽核推翻**：公司同日 Steven 20260519 改寫只做一半。(1) V910 `FormShow:72` 每次 timer refresh 都強制清掉使用者的「不存圖」勾選 → 靜默恢復全螢幕 JPG 寫檔；V899 有存/還原。(2) V910 `SaveJPG` 把 allocation 與 release 都放在 try 內，`SaveToFile` 拋例外（磁碟滿／權限——正是本案要修的情境）時 release 整段跳過 → 每次失敗洩漏 ~8MB TBitmap＋TCanvas＋TJPEGImage＋未釋放的螢幕 DC；且缺 `if(hDc!=NULL)` 檢查 | (a) 補搬 V899 版（NULL 先宣告、release 移出 catch、還原勾選） (b) 維持 V910 半套版 | **20260901 已補搬（使用者裁決）**：(a) Timer1Timer 以 V899 原樣還原 bEnterSave 與 CheckBox1 勾選；(b) SaveJPG 四個資源改 NULL 先宣告、內層 try、釋放區塊移出並逐一 null 檢查、補 hDc!=NULL 守衛（保留 V910 外層 try 的關窗保護）|
| **F14 ✅ DONE** | 20260831 Shake Shuttle Time Out 案（`ainarm2.cpp` ×3、`ainarm2.h` ×1、`ckernel.cpp` ×1） | 戰役後（20260831）的新案，**V910 完全沒有**：`ResetShakeShuttleTimeOut` 全樹 0 命中；V910 `ckernel.cpp` 同位置只有 20260525 RogerYang 的 `ResetShtMoveTimeoutWatchdog()`（不同計時器不同案）；診斷字串 `fCanMoveM:d` 少一個 `%` 的 bug 也還在。**附帶發現**：V910 `ainarm2.cpp:2390` 誤寫 `str3.sprintf(...)`（V899 是 `str2`）→ `ShowMyMessage(str1, str2)` 傳未初始化的 str2，第二行訊息在 V910 是空的 | (a) 補搬（5 條，含順手修好 V910 的 str2/str3 筆誤） (b) 不搬 | **20260901 已補搬（使用者裁決）**：4 個 op 全數位元組級搬入（新函式＋.h 宣告＋ckernel resume 呼叫＋診斷字串）。**附帶修好 V910 的 str3/str2 筆誤**（原本 ShowMyMessage 第二行是空的）|
| F12 | MG-W16 的 cShowBinSelect 三條（0415/0417 UI 動態堆疊，互相耦合） | V910 有**同日不同作者的競品解法**（KevinCheng 20260415「避免遮住按鈕」），且 V910 .dfm 多一顆 V899 沒有的 btnASM（Top=229）——V899 堆疊起點 233，照搬＝把 KevinCheng 修的按鈕遮蔽 bug 搬回去；另 GIGAS 可能失去該面板、分頁高度 450→714 | (a) 不搬（保留 V910 解法） (b) 只擴顯示條件為 CC_GIGAS \|\| SPIL_FOR_QLE \|\| bAutoDeviceEjection（V910 現為 write-only 死旗標，此選項會點活它） | **建議 (a) 不搬**，已照建議執行；細節 mg_w16_analysis.md §6.1 |
| F2 | MG-W5 已搬（9c7b8e1 之後的 W5 commit）：HS_Function ProdLog/GPIBLog「跨午夜換昨天」（0415） | V899 的 swap 是**無條件**的：修好 method 0（00:00 上傳抓昨天檔=正確），但 method 1（20:00）/method 2（每整點）/method 3 會從「抓今天」變「抓昨天」，且與同函式 JamStat 的 hour-aware 基準不一致。V899 出貨版現況即如此（忠實搬運已完成，V910=V899 行為一致） | (a) 維持忠實現狀 (b) 改成 hour-aware（只在 00:00 附近才換昨天）——屬**改 V899 行為**，兩樹要一起改 | **建議 (a) 維持**；若 KYEC/矽格實際在用 method 1/2 且反映抓錯日，再開 V899 案處理（那是 V899 議題非搬移議題） |
